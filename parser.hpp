#ifndef __PARSER_HPP
#define __PARSER_HPP

#include <iostream>

class Parser
{
    private:
        static const size_t MAX_LEN = 1000;

        Parser() = default;

    public:
        static void useParser();
};

#endif
